<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <%= link_to "Blog" , root_path, class: "navbar-brand" %>
        <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarMenuItems">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarMenuItems">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <%= link_to "Articulos", articles_path, class: "nav-link active" %>
                </li>
            </ul>
            <% if user_signed_in? %>
                <ul class="navbar-nav mb-2 mb-lg-0">
                    <li class="nav-item">
                        <%= button_to "Cerrar Sesion", destroy_user_session_path, method: :delete, class: "nav-link active" %>
                    </li>
                </ul>
            <% end %>
        </div>
    </div>
</nav>
